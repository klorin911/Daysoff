@page "/signin"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // This endpoint used to accept identity via query string, which was insecure.
        Nav.NavigateTo("/login?error=Please%20sign%20in%20using%20the%20login%20form.", forceLoad: true);
    }
}
